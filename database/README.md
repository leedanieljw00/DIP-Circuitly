# Database Folder

This folder is reserved for:
- Database configuration files.
- SQL scripts to create tables.
- Migration files for PostgreSQL.

Currently, the app uses local data services, but this structure is ready for a real database upgrade.
